import { MentionsDirective } from './mentions.directive';

describe('MentionsDirective', () => {
  it('should create an instance', () => {
    const directive = new MentionsDirective();
    expect(directive).toBeTruthy();
  });
});
